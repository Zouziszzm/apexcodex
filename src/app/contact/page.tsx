import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact | Apexcodex",
  description: "Get in touch with me.",
};

export default function Page() {
  return <ContactClient />;
}
